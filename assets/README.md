# Assets for directory submissions

- `logo.png`: square 180x180 product icon (copied from the app's apple-touch-icon). Several directories want 256x256 or larger; export a bigger square PNG from the source SVG if a directory rejects this one.
- `screenshot.png`: 1512x795 dashboard screenshot (stat tiles + global check network map), captured 2026-07-07.
